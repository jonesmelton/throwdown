<%= form_for ([@team, @registration]) do |f| %>
<%= f.label :player %>
<%= f.select (:player_id) do %>
  <% players = User.all.map { |user| [user.username, user.id] } %>
  <%= options_for_select(players) %>
  <%= f.submit %>
  <% end %>
<% end %>
