
  <table class="table">
    <tr>
      <th>Team Name</th>
      <th>Players</th>
    </tr>
  <% @user.captained_teams.each do |team| %>
    <tr>
      <td><%= link_to team.name, team %></td>
      <td><%= team.players.count %></td>
    </tr>
    <% end %>
  </table>
