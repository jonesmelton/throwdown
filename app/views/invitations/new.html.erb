<%= form_for @invitation do |f| %>
  <%= f.label :user %>
  <%= f.select (:user_id) do %>
    <% players = @game.untouched_participants.map { |user| [user.username, user.id]} %>
    <%= options_for_select(players) %>
    <%= f.submit "Send Invitation" %>
    <% end %>
  <% end %>
