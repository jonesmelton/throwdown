<%= form_for @invitation do |f| %>
  <%= f.label :user %>
  <%= f.select (:user_id) do %>
    <% players = @team.players.all.map { |user| [user.username, user.id] } %>
    <%= options_for_select(players) %>
    <%= f.submit "Remove Registration" %>
    <% end %>
  <% end %>
